# Map section:


# Write a Python program to triple all numbers of a given list of integers. Use Python map

# list_of_int = [1, 2, 3, 4, 5]

# print(list(map(lambda x: x * 3, list_of_int)))

# Write a Python program to square the elements of a list using map() function.

# list_of_int = [1, 2, 3, 4, 5]

# print(list(map(lambda x: x**2, list_of_int)))

# Write a Python program to add three given lists using Python map and lambda

# first_list = [1, 2, 3, 4, 5]
# second_list = [6, 7, 8, 9, 10]
# third_list = [11, 12, 13, 14, 15]

# print(list(map(lambda a, b, c: a + b + c, first_list, second_list, third_list)))

# Write a Python program to add two given lists and find the difference between lists. Use map() function.

# first_list = [1, 2, 3, 4, 5]
# second_list = [6, 7, 8, 9, 10]

# print(list(map(lambda a, b: a + b, first_list, second_list)))

# print(list(map(lambda a, b: a - b, first_list, second_list)))

# Write a Python program to convert a given list of integers and a tuple of integers in a list of strings.

# list_of_ints = [1, 2, 3, 4, 5]
# tuple_of_strings = ("one", "two", "three", "four", "five")

# print(list(map(lambda x, y: str(x) + " " + str(y), list_of_ints, tuple_of_strings)))
